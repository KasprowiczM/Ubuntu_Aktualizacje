# Ascendo — English catalog
# Lines: key = value.  '#' starts a comment.

# ── Generic / status ────────────────────────────────────────────────────
status.ok        = ok
status.warn      = warn
status.err       = error
status.skip      = skip
status.info      = info
status.unknown   = ?

action.add       = add
action.remove    = remove
action.upgrade   = upgrade
action.install   = install
action.refresh   = refresh

# ── Setup / fresh-machine ───────────────────────────────────────────────
setup.welcome     = Welcome to Ascendo — unified system updates.
setup.lang_pick   = Pick the interface language for CLI and dashboard:
setup.lang_choose = Choose [%s]:
setup.lang_set    = Language set to: %s
setup.preflight   = Step 1/5 — preflight host audit
setup.bootstrap   = Step 2/5 — overlay restore + setup
setup.dashboard   = Step 3/5 — dashboard Python venv
setup.service     = Step 4/5 — dashboard user-service
setup.verify      = Step 5/5 — state verification
setup.next_steps  = Next steps
setup.ask_apps    = Run \`ascendo apps detect\` to compare your installed apps with this configuration.

# ── update-all ──────────────────────────────────────────────────────────
update.start          = System update — %s
update.profile        = Profile
update.phases         = Phases
update.run_id         = Run id
update.dry_run_notice = DRY RUN — apply/verify/cleanup phases skipped
update.complete       = Complete — %s
update.reboot.box     = RESTART REQUIRED
update.reboot.now     = Run now:    sudo systemctl reboot
update.reboot.later   = Or later:   sudo shutdown -r +5
update.reboot.dash    = Dashboard: open http://127.0.0.1:8765 — banner shows a one-click 'Restart now' button.
update.sudo.prompt    = Sudo password (asked ONCE for the whole run):
update.sudo.label     = [sudo] password for %s:
update.sudo.wrong     = Wrong password, try again.

# ── Apps detect / register ──────────────────────────────────────────────
apps.title         = Detected applications
apps.col_cat       = Category
apps.col_name      = Package
apps.col_state     = State
apps.col_action    = Suggested
apps.state.tracked   = tracked
apps.state.untracked = detected
apps.state.missing   = missing
apps.action.add      = ascendo apps add %s --category %s
apps.action.install  = ascendo apps install-missing
apps.summary       = %d tracked · %d detected (not in config) · %d missing
apps.no_changes    = No new applications detected.

# ── Dev sync ────────────────────────────────────────────────────────────
sync.col_path     = Path
sync.col_action   = Action
sync.col_size     = Size
sync.col_status   = Status
sync.title.export = Dev-sync export — overlay → provider
sync.summary      = Files: %d total, %d ok, %d failed
sync.skipped      = skipped (excluded)
sync.failed_meta  = metadata-limited (Read-only file system)
