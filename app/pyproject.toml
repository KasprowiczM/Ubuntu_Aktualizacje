[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ascendo-ubuntu-dashboard"
version = "0.4.0"
description = "Local dashboard for Ascendo Ubuntu update suite"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.7",
]

[project.scripts]
ascendo-ubuntu-dashboard = "app.backend.__main__:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
